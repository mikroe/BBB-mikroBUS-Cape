Here are all the tools to make the CAN Click work with the mikrobus shield. 

  $ mkdir can-utils
  $ tar  -C /BBB-mikroBUS-Cape/Examples/CAN-click/can-utils -xvf can-utils.tar
  $ cd can-utils/
  $ make 
  $ make install